DATABASE_URL="postgresql://dhibob:dhibob_secret@localhost:5432/dhibob"
TEST_DATABASE_URL="postgresql://dhibob:dhibob_secret@localhost:5432/dhibob_test"
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (optional — login page shows Google button only when both are set)
# Create credentials at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Field-level encryption for PII (nationalId, passportNumber, bankAccount)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FIELD_ENCRYPTION_KEY=
REDIS_URL="redis://localhost:6379"

# External notifications (optional — dispatcher silently skips a channel if its vars are missing)
# Resend — https://resend.com (free tier up to 3k emails/month)
RESEND_API_KEY=
NOTIFICATION_FROM_EMAIL="Dpeople <notifications@develeap.com>"
# Slack bot — see docs/slack-bot-setup.md for scopes and install steps
SLACK_BOT_TOKEN=
# Google Calendar sync (optional -- creates all-day events when time-off is approved)
# Service account JSON key, base64-encoded: cat key.json | base64 -w0
GOOGLE_SERVICE_ACCOUNT_KEY=
# Calendar ID: use "primary" for dev, or a shared calendar ID for prod
GOOGLE_CALENDAR_ID=primary
# Base URL used in outgoing notification links (falls back to http://localhost:3000)
NEXT_PUBLIC_APP_URL=http://localhost:3000
